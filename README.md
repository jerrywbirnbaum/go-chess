Go Chess project

To run locally


START UI
cd go-chess-react
npm run dev

Start backend
go run .

Then go to http://localhost:5173

To run with Docker Compose

docker compose up --build

Then go to http://localhost:5173
